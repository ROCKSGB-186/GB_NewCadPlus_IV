<Window
    x:Class="GB_NewCadPlus_IV.Views.InsertGraphicPropertyWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="编辑图元属性"
    Width="940"
    Height="620"
    MinWidth="680"
    MinHeight="360"
    WindowStartupLocation="CenterOwner"
    ResizeMode="CanResize"
    ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Margin="0,0,0,10"
            FontSize="15"
            FontWeight="Bold"
            Text="插入前图元属性（可编辑）" />

        <DataGrid
            x:Name="PropertiesGrid"
            Grid.Row="1"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            HeadersVisibility="Column"
            IsReadOnly="False"
            ItemsSource="{Binding Properties}"
            SelectionUnit="CellOrRowHeader">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="220"
                    Binding="{Binding Name}"
                    Header="属性 Tag"
                    IsReadOnly="True" />
                <DataGridTextColumn
                    Width="180"
                    Binding="{Binding Comment}"
                    Header="注释"
                    IsReadOnly="True" />
                <DataGridTemplateColumn Width="*" Header="属性值"><DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding Value}" /></DataTemplate></DataGridTemplateColumn.CellTemplate><DataGridTemplateColumn.CellEditingTemplate><DataTemplate><Grid><TextBox Text="{Binding Value, UpdateSourceTrigger=LostFocus}"><TextBox.Style><Style TargetType="TextBox"><Setter Property="Visibility" Value="Visible" /><Style.Triggers><DataTrigger Binding="{Binding IsConnectionType}" Value="True"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger></Style.Triggers></Style></TextBox.Style></TextBox><ComboBox ItemsSource="{Binding Options}" SelectedItem="{Binding Value, UpdateSourceTrigger=PropertyChanged}"><ComboBox.Style><Style TargetType="ComboBox"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsConnectionType}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></ComboBox.Style></ComboBox></Grid></DataTemplate></DataGridTemplateColumn.CellEditingTemplate></DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel
            Grid.Row="2"
            Margin="0,12,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                x:Name="BtnCancel"
                Width="100"
                Margin="0,0,10,0"
                Padding="8,4"
                Click="BtnCancel_Click"
                Content="取消"
                IsCancel="True" />
            <Button
                x:Name="BtnInsert"
                Width="120"
                Padding="8,4"
                Click="BtnInsert_Click"
                Content="插入图元"
                IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
